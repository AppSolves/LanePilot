services:
  raspberry:
    restart: always
    platform: linux/arm64
    image: ghcr.io/appsolves/lanepilot:raspberrypi-latest
    network_mode: host
    logging:
      driver: "json-file"
      options:
        max-size: "25m"
        max-file: "3"